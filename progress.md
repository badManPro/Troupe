# Progress

- 2026-04-16: 新任务开始，目标是为 AI 输出的流程类 Markdown 章节增加“预览图”按钮，保留原文并按需生成流程图。
- 2026-04-16: 已定位聊天与文档的 Markdown 渲染入口，决定在标题节点旁挂载通用预览按钮，而不是改写正文内容。
- 2026-04-16: 已新增 Markdown 章节抽取与候选判定逻辑，支持根据标题关键词和列表结构识别可视化段落。
- 2026-04-16: 已新增 Mermaid 预览接口，复用当前 AI provider 将单个章节即时转换为 `flowchart LR`。
- 2026-04-16: 已新增 Mermaid 渲染组件、预览弹窗组件，并接入聊天消息和文档预览两个渲染器。
- 2026-04-16: 依赖安装过程中 `npm install mermaid` 失败，最终改用 `pnpm add mermaid` 完成安装。
- 2026-04-16: `node node_modules/typescript/bin/tsc --noEmit --pretty false` 通过。
- 2026-04-16: 根据用户反馈继续排查，确认旧候选规则过宽，导致普通标题后也出现预览按钮。
- 2026-04-16: 已收紧规则为“显式流程标题或强顺序动作段落”，并排除了顶层标题、带嵌套子标题的汇总章节。
- 2026-04-16: 已把按钮从标题同行移到标题下方的独立操作位，减少正文阅读干扰。
- 2026-04-16: 已用本地运行中的 `Next` 接口直接验证 `/api/diagram-preview`，返回了有效 Mermaid 数据。
- 2026-04-16: 已将 Mermaid 渲染改为浏览器端动态加载，并在渲染失败时增加源码回退显示。
- 2026-04-16: 根据用户截图继续定位，确认白板弹窗的根因是预览请求没有在弹窗打开时真正发起。
- 2026-04-16: 已将预览触发改为 `open` 状态驱动的 `useEffect`，并补充空状态占位，避免再次出现完全空白的弹窗主体。
