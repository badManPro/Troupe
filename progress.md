# Progress

- 2026-04-15: 新任务开始，目标是修复应用重启后 AI 对话历史与上下文丢失的问题。
- 2026-04-15: 已确认 `conversations` / `messages` 表与恢复接口都存在，问题不在消息写库逻辑。
- 2026-04-15: 已定位到数据库与迁移目录都依赖 `process.cwd()`，这会导致桌面端重启后可能指向新的空库。
- 2026-04-15: 已新增数据库路径解析模块，运行时与 Drizzle CLI 统一采用“环境变量 > 旧库路径 > 稳定用户数据目录”的兼容策略。
- 2026-04-15: 已补充旧库探测逻辑，除 `cwd` 外也会回溯项目根目录里的 `troupe.db`，减少历史数据断档。
- 2026-04-15: 已将迁移目录解析从 `process.cwd()` 改为可回溯项目根目录，降低桌面端重启后的路径漂移风险。
- 2026-04-15: 已验证“模拟切换启动目录”场景下，数据库会回到仓库根目录旧库，而迁移目录仍可定位到 `.next/standalone/drizzle`。
- 2026-04-15: 根据用户复现场景进一步确认，返回项目列表再进入时的主因是前端恢复时序竞态，而不是数据库未写入。
- 2026-04-15: 已修改项目页会话恢复逻辑，先完整取回 conversation 与 messages，再挂载 `ChatPanel`；同时增加恢复中的占位态和失败重试入口。
- 2026-04-15: `npx tsc --noEmit` 通过。
